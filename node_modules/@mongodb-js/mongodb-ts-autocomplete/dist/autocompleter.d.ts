import type { AutoCompletion } from '@mongodb-js/ts-autocomplete';
import type { JSONSchema } from 'mongodb-schema';
import type { AutocompletionContext } from './autocompletion-context';
type MongoDBAutocompleterOptions = {
    context: AutocompletionContext;
};
declare class ConnectionSchema {
    private readonly databaseSchemas;
    constructor();
    addDatabase(databaseName: string): void;
    setDatabaseCollectionNames(databaseName: string, collectionNames: string[]): void;
    addCollectionSchema(databaseName: string, collectionName: string, collectionSchema: JSONSchema): void;
    toTypescriptTypeDefinition(): string;
}
export declare class MongoDBAutocompleter {
    private readonly context;
    private readonly connectionSchemas;
    private readonly autocompleter;
    constructor({ context }: MongoDBAutocompleterOptions);
    addConnection(connectionId: string): ConnectionSchema;
    getConnectionCode(connectionId: string): string;
    getCurrentGlobalsCode(connectionId: string, databaseName: string): string;
    autocomplete(code: string): Promise<AutoCompletion[]>;
}
export {};
//# sourceMappingURL=autocompleter.d.ts.map